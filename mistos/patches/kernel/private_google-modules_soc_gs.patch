diff --git a/drivers/usb/dwc3/core-exynos.h b/drivers/usb/dwc3/core-exynos.h
index b265c39..ffb5494 100644
--- a/drivers/usb/dwc3/core-exynos.h
+++ b/drivers/usb/dwc3/core-exynos.h
@@ -14,7 +14,7 @@
 
 /* LINK Registers */
 #define DWC3_LSKIPFREQ		0xd020
-#define DWC3_LLUCTL		0xd024
+#define DWC3_LLUCTL_EXYNOS		0xd024
 
 /* Bit fields */
 /* Link Register - LLUCTL */
diff --git a/drivers/usb/dwc3/dwc3-exynos.c b/drivers/usb/dwc3/dwc3-exynos.c
index c0af4e0..78195e5 100644
--- a/drivers/usb/dwc3/dwc3-exynos.c
+++ b/drivers/usb/dwc3/dwc3-exynos.c
@@ -255,17 +255,17 @@ static void dwc3_core_config(struct dwc3 *dwc, struct dwc3_exynos *exynos)
 	dwc3_exynos_writel(dwc->regs, DWC3_GUSB3PIPECTL(0), reg);
 
 	if (DWC3_VER_IS(DWC31, 170A)) {
-		reg = dwc3_exynos_readl(dwc->regs, DWC3_LLUCTL);
+		reg = dwc3_exynos_readl(dwc->regs, DWC3_LLUCTL_EXYNOS);
 		reg |= (DWC3_PENDING_HP_TIMER_US(0xb) | DWC3_EN_US_HP_TIMER);
 		reg |= DWC3_FORCE_GEN1;
-		dwc3_exynos_writel(dwc->regs, DWC3_LLUCTL, reg);
+		dwc3_exynos_writel(dwc->regs, DWC3_LLUCTL_EXYNOS, reg);
 
 		reg = dwc3_exynos_readl(dwc->regs, DWC3_LSKIPFREQ);
 		reg |= (DWC3_PM_ENTRY_TIMER_US(0x9) |
 			DWC3_PM_LC_TIMER_US(0x5) | DWC3_EN_PM_TIMER_US);
 		dwc3_exynos_writel(dwc->regs, DWC3_LSKIPFREQ, reg);
 	} else if (DWC3_VER_IS_WITHIN(DWC31, 180A, 190A)) {
-		reg = dwc3_exynos_readl(dwc->regs, DWC3_LLUCTL);
+		reg = dwc3_exynos_readl(dwc->regs, DWC3_LLUCTL_EXYNOS);
 		reg &= ~(DWC3_LLUCTL_TX_TS1_CNT_MASK);
 		reg |= (DWC3_PENDING_HP_TIMER_US(0xb) | DWC3_EN_US_HP_TIMER) |
 		    (DWC3_LLUCTL_PIPE_RESET) | (DWC3_LLUCTL_LTSSM_TIMER_OVRRD) |
@@ -274,7 +274,7 @@ static void dwc3_core_config(struct dwc3 *dwc, struct dwc3_exynos *exynos)
 		if (exynos->config.force_gen1 && exynos->force_speed != USB_SPEED_SUPER_PLUS)
 			reg |= DWC3_FORCE_GEN1;
 
-		dwc3_exynos_writel(dwc->regs, DWC3_LLUCTL, reg);
+		dwc3_exynos_writel(dwc->regs, DWC3_LLUCTL_EXYNOS, reg);
 
 		reg = dwc3_exynos_readl(dwc->regs, DWC3_LSKIPFREQ);
 		reg &= ~(DWC3_PM_LC_TIMER_US_MASK |
diff --git a/drivers/watchdog/s3c2410_wdt.c b/drivers/watchdog/s3c2410_wdt.c
index a6b5729..2f19a99 100644
--- a/drivers/watchdog/s3c2410_wdt.c
+++ b/drivers/watchdog/s3c2410_wdt.c
@@ -720,7 +720,7 @@ static int proportion_in_1_digit(u64 dividend, u64 divisor)
 void s3c2410wdt_print_schedstat(const char *loglvl)
 {
 	int cpu;
-	char buf[max(NR_CPUS, PRINT_CPUS_LIMIT)];
+	char buf[(NR_CPUS > PRINT_CPUS_LIMIT ? NR_CPUS : PRINT_CPUS_LIMIT)]; /* was max(), not a constant-expr for VLA purposes */
 	char *bufp;
 	u64 duration;
 	struct s3c2410_wdt *wdt = s3c_wdt[LITTLE_CLUSTER];
diff --git a/include/linux/usb/dwc3-exynos.h b/include/linux/usb/dwc3-exynos.h
index 02ee552..03146ab 100644
--- a/include/linux/usb/dwc3-exynos.h
+++ b/include/linux/usb/dwc3-exynos.h
@@ -27,7 +27,7 @@
 /* LINK Registers */
 #define DWC3_LU3LFPSRXTIM	0xd010
 #define DWC3_LSKIPFREQ		0xd020
-#define DWC3_LLUCTL		0xd024
+#define DWC3_LLUCTL_EXYNOS		0xd024
 #define DWC3_BU31RHBDBG		0xd800
 
 /* Link Register - LLUCTL */
